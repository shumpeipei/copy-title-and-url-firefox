# 検討仕様書: 開いているページのタイトルと URL をコピーする

状態: 仕様確定。実装準備完了。
対象: 開いているページのタイトルと URL のコピー。推奨する実現手段は、このリポジトリの Firefox 拡張機能。
結論: 指定の2行形式を、ページ種を問わず短く起動してコピーするなら、小さな拡張機能がよい。Firefox 標準機能ではこの形式を作れない。URL だけでよいなら拡張機能は不要。

## 1. 目的

開いているページのタイトルと URL を、次の形でクリップボードへコピーする。

```
タイトル
URL
```

「タイトル」「URL」はラベルではない。それぞれ、その時点のページタイトルとページ URL に置き換わる。

コピー例:

```
Mozilla Firefox
https://www.mozilla.org/
```

## 2. 方式の比較

Firefox に、タイトルと URL をこの形式でコピーする標準コマンドはない。アドレスバーを選択してコピー（Ctrl+L、Ctrl+C）できるのは URL だけ。

| 方式 | この形式 | 向く条件 | 向かない条件 |
| --- | --- | --- | --- |
| 標準操作 | 不可。URL のみ | URL だけでよい | 今回の形式 |
| ブックマークレット | https の通常ページでは可能なことが多い。ページ上の `document.title` と `location.href` を書く | たまに使う。対象が通常の https ページに限られる。ブックマークのクリックでよい | 1キー起動。`http:`、`about:`、PDF、リーダービュー。ページコンテキストなので特権ページでは実行できない。`http:` は secure context ではなく Clipboard API が使えない |
| ユーザースクリプト | ブックマークレットと同じ制限。管理用の拡張機能が別途必要 | すでにマネージャを常用している | この1機能のためだけに足す |
| OS 側 | 不可。Hyprland から見えるウィンドウタイトルはページタイトルであり、URL を含まない | タイトルだけでよい | 今回の形式。URL をブラウザの外から安定して取れない |
| userChrome.js / autoconfig | 技術的には可能 | 使わない。非サポートで Firefox の更新に弱い | |
| 既存アドオン | 可能 | 自作せず、相手の形式と権限でよい | 形式を固定し、権限を `activeTab` と `clipboardWrite` に抑えたい。既存アドオンは広いホスト権限を求めることがあり、形式も選べない |
| 自作拡張機能 | 可能。タブ API なのでページへ注入しない | 形式を固定し、ページ種を問わず、起動を短くしたい | 年に数回、https のクリックだけでよい |

推奨は自作拡張機能。以降の節はこの前提。ブックマークレットに切り替えるなら、3 節以降の権限と実装方式は捨てる。

Firefox はブックマークにキーボードショートカットを直接割り当てられない。キーワードを付けてアドレスバーから起動する方法はあるが、ページを見ながらの1キー操作にはならない。

## 3. コピー仕様

確定していること:

- 1 行目はページタイトル、2 行目はページ URL。
- 区切りは改行 1 つ（LF、`\n`）。CRLF は要求されていない。
- ラベル、Markdown リンク、引用符、空行は付けない。
- 末尾に余分な改行は付けない。文字列は URL の最後の文字で終わる。
- タイトルと URL はタブ API が返す値をそのまま使う。トリム、エンコード、正規化はしない。
- URL はアドレスバーに対応する `tabs.Tab.url` とする。コンテンツスクリプトの `location` は使わない。query と hash も含む。

- タイトルまたは URL が空、または権限上読めないときはコピーを行わず、ツールバーバッジでエラー表示（`ERR`）を行う。不完全な文字列はクリップボードに書き込まない。

## 4. 実現方式

ユーザーが拡張機能を起動したとき、そのタブの `title` と `url` を読み、`タイトル\nURL` を `navigator.clipboard.writeText()` で書く。

根拠:

- `activeTab` は、ユーザー操作で拡張機能が起動されたタブに限り、`Tab.url` と `Tab.title` を読める。
  https://developer.mozilla.org/en-US/docs/Mozilla/Add-ons/WebExtensions/manifest.json/permissions
- `clipboardWrite` があれば、transient activation なしで拡張機能のコンテキストから `Clipboard.writeText()` できる。Firefox では content script を含む拡張機能コンテキストで有効。
  https://developer.mozilla.org/en-US/docs/Mozilla/Add-ons/WebExtensions/Interact_with_the_clipboard

実装上の制約:

- Manifest V3。バックグラウンドは Firefox の `background.scripts`（event page）とする。`service_worker` は使わない。Chrome の service worker には `navigator.clipboard` が無く、同じ落とし穴を避ける。
- クリップボード書き込みは content script では行わない。`http:` ページは secure context ではなく、Clipboard API が使えない。
- `document.execCommand("copy")` は非推奨のため使わない。
- ツールバーボタンをワンクリックのコピーにする場合、`action.default_popup` は置かない。ポップアップがあると `action.onClicked` は発火しない。
- 全サイト常時アクセスの `tabs` 権限と `<all_urls>` は、この機能だけでは不要。
- Firefox MV3 のため、`manifest.json` の `browser_specific_settings.gecko.id` を必須とする（一時読み込みや署名時のバリデーションエラー回避）。
- バッジ表示時は必ず対象の `tabId` を指定し、他タブ・他ウィンドウへの波及を防ぐ。
- 連続クリック時のバッジ消去競合を防ぐため、`tabId` ごとにタイマー（`clearTimeout`）を管理する。

想定する中核処理:

```js
const text = `${tab.title}\n${tab.url}`;
await navigator.clipboard.writeText(text);
```

`tab` は、ユーザー操作で渡されるアクティブタブ、または `browser.tabs.query({ active: true, currentWindow: true })` の結果。ボタンの `onClicked` がタブを渡すなら、追加の query は不要。

## 5. 権限

| 権限 | 要否 | 理由 |
| --- | --- | --- |
| `activeTab` | 必要 | 起動したタブの `title` と `url` だけを読む。常時のタブ監視はしない。 |
| `clipboardWrite` | 必要 | 非同期でタブ情報を取った後でも、バックグラウンドから確実に書き込む。 |
| `tabs` | 不要 | `activeTab` で足りる。インストール時の権限説明を重くしない。 |
| host permissions | 不要 | ページ内容へアクセスしない。 |

`activeTab` が付くユーザー操作は、ツールバーボタン、コンテキストメニュー、拡張機能が定義したキーボードショートカット（Firefox 63 以降）、拡張機能ページ上のボタン。今回の機能はこの範囲で足りる。

## 6. 起動方法とフィードバック

- 起動方法: ツールバーボタン（クリックで即時コピー）。ポップアップは設置せず `browser.action.onClicked` を使用する。
  - 補足: `commands` API で `_execute_action` を定義し、キーボードショートカット（`Alt+Shift+C`）でも同一のアクションをトリガー可能とする。
- フィードバック: ツールバーバッジ（`browser.action.setBadgeText`）を `tabId` 指定で一時表示（約 1.5 秒）して自動消去する。
  - 成功時: `OK`（背景色: `#28a745`）
  - 失敗時（タイトル/URL 取得不可、クリップボード書き込み失敗）: `ERR`（背景色: `#dc3545`）

## 7. 制約と例外

- 読み込み中のタブは、タイトルが空、または前のページのタイトルのことがある。URL も一瞬 `about:blank` になりうる。タイトルまたは URL が空・未取得の場合は書き込まず `ERR` 表示とする。
- プライベートウィンドウでは、ユーザーがその拡張機能のプライベートウィンドウでの実行を許可している必要がある。拡張機能側から強制はできない。
- `about:` や PDF ビューア、リーダービューは、タブ API が返す URL（例: `about:reader?url=...`）を加工せずそのままコピーする。
- 特権ページ（`about:config` 等）で `url` / `title` が取得できない場合も、ガードにより安全に `ERR` 表示となる。
- `activeTab` は、ユーザーが起動した時点のタブにしか付かない。別タブを常時監視する機能には使えない。今回の要件には合っている。
- 複数ウィンドウでは、操作したウィンドウのアクティブタブを対象にする。`currentWindow: true` を使う。

## 8. 対象外

この検討では次を要件にしない。

- 選択中のテキストのコピー
- 複数タブ、ウィンドウ内の全タブのコピー
- Markdown リンク、HTML、リッチテキストでのコピー
- ページ本文の取得、コンテンツスクリプトの注入
- Chrome など他ブラウザへの対応
- コピー履歴、設定画面、コピー形式の切り替え
- リーダービュー等の元 URL 復元・パラメータ加工

## 9. 決定事項

1. 起動方法: 初版は「ツールバーボタン」とし、`commands` API（`_execute_action`、デフォルト `Alt+Shift+C`）を併用定義する。
2. フィードバック: `browser.action.setBadgeText` による一時バッジ表示（`tabId` 指定、成功時 `OK`、失敗時 `ERR`）を採用する。追加権限（`notifications`）は不要。
3. 特殊 URL（リーダービュー・PDF 等）の扱い: タブ API から取得した文字列を加工せずそのまま出力する。
4. 異常系の扱い: タイトルまたは URL が欠落している場合、クリップボードへの書き込みは行わず、`ERR` バッジを表示する。
5. 実装上の必須要件: `browser_specific_settings.gecko.id` の明記、バッジ消去タイマーの競合制御（`clearTimeout`）。
